worker_processes  1;

events {
    worker_connections  1024;
}

http {
    server {
        listen       3000;
        server_name  localhost;
        root /usr/share/nginx/html/;
        location / {
            try_files $uri /index.html;
        }
    }
}
